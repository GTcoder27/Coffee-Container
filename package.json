{
  "name": "coffee-container",
  "version": "1.0.0",
  "description": "Coffee Container - Secret File Sharing Application",
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm run start --prefix backend",
    "dev": "cd frontend && npm run build && cd ../backend && node app.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
